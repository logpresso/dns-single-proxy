<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.logpresso.dnsproxy" level="INFO"/>

    <!-- Set to DEBUG to see individual queries/responses -->
    <logger name="com.logpresso.dnsproxy.server.DnsHandler" level="INFO"/>
    <logger name="com.logpresso.dnsproxy.cache.DnsCache" level="INFO"/>
    <logger name="com.logpresso.dnsproxy.filter.SingleRecordFilter" level="INFO"/>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
